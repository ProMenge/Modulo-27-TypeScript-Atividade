const multiply = (number1: number, number2: number): number => { return number1 * number2 }

const helloName = (name: string): string => {
    return ('Hello ' + name);
}

console.log(helloName('Fred'))
console.log(multiply(5, 9));